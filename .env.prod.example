# Production env — copy to .env on the server and fill in the password.
# Do NOT commit the real .env (it's gitignored).
DB_HOST=database-1.cz8aquy4me96.us-east-2.rds.amazonaws.com
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=
# Tables were loaded into the "postgres" database. If you create a dedicated
# "rsvp" database and load schema.sql there, change this to rsvp.
DB_NAME=postgres
DB_SSLMODE=require
